import PAGE_ROUTES from './PAGE_ROUTES';
import REQUEST_RETRY_COUNT from './REQUEST_RETRY_COUNT';

// eslint-disable-next-line import/prefer-default-export
export { PAGE_ROUTES, REQUEST_RETRY_COUNT };
